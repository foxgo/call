server:
  port: 8081

spring:
  application:
    name: call-ingestion
  threads:
    virtual:
      enabled: true
  jackson:
    default-property-inclusion: non_null
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:localhost:9876}
  producer:
    group: ${ROCKETMQ_PRODUCER_GROUP:call-producer-group}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

call:
  rocketmq:
    name-server: ${ROCKETMQ_NAME_SERVER:localhost:9876}
    producer-group: ${ROCKETMQ_PRODUCER_GROUP:call-producer-group}
    topics:
      record-ingest: ${CALL_RECORD_INGEST_TOPIC:call_record_ingest}
      round-ingest: ${CALL_ROUND_INGEST_TOPIC:call_round_ingest}
    consumers:
      record:
        group: ${CALL_RECORD_CONSUMER_GROUP:call-record-consumer-group}
        consume-thread-max: ${CALL_RECORD_CONSUMER_THREADS:6}
        max-reconsume-times: ${CALL_RECORD_MAX_RECONSUME_TIMES:3}
      round:
        group: ${CALL_ROUND_CONSUMER_GROUP:call-round-consumer-group}
        consume-thread-max: ${CALL_ROUND_CONSUMER_THREADS:6}
        max-reconsume-times: ${CALL_ROUND_MAX_RECONSUME_TIMES:3}
      index:
        group: ${CALL_INDEX_CONSUMER_GROUP:call-index-group}
        consume-thread-max: ${CALL_INDEX_CONSUMER_THREADS:2}
        max-reconsume-times: ${CALL_INDEX_MAX_RECONSUME_TIMES:3}
      record-dlq:
        group: ${CALL_RECORD_DLQ_CONSUMER_GROUP:call-record-dlq-group}
        consume-thread-max: ${CALL_RECORD_DLQ_CONSUMER_THREADS:1}
        max-reconsume-times: ${CALL_RECORD_DLQ_MAX_RECONSUME_TIMES:3}
      round-dlq:
        group: ${CALL_ROUND_DLQ_CONSUMER_GROUP:call-round-dlq-group}
        consume-thread-max: ${CALL_ROUND_DLQ_CONSUMER_THREADS:1}
        max-reconsume-times: ${CALL_ROUND_DLQ_MAX_RECONSUME_TIMES:3}
      index-dlq:
        group: ${CALL_INDEX_DLQ_CONSUMER_GROUP:call-index-dlq-group}
        consume-thread-max: ${CALL_INDEX_DLQ_CONSUMER_THREADS:1}
        max-reconsume-times: ${CALL_INDEX_DLQ_MAX_RECONSUME_TIMES:3}
      ai:
        group: ${CALL_AI_CONSUMER_GROUP:call-ai-group}
        consume-thread-max: ${CALL_AI_CONSUMER_THREADS:2}
        max-reconsume-times: ${CALL_AI_MAX_RECONSUME_TIMES:3}
      ai-dlq:
        group: ${CALL_AI_DLQ_CONSUMER_GROUP:call-ai-dlq-group}
        consume-thread-max: ${CALL_AI_DLQ_CONSUMER_THREADS:1}
        max-reconsume-times: ${CALL_AI_DLQ_MAX_RECONSUME_TIMES:3}
      third-party:
        group: ${CALL_THIRD_PARTY_CONSUMER_GROUP:call-third-party-group}
        consume-thread-max: ${CALL_THIRD_PARTY_CONSUMER_THREADS:2}
        max-reconsume-times: ${CALL_THIRD_PARTY_MAX_RECONSUME_TIMES:3}
      third-party-dlq:
        group: ${CALL_THIRD_PARTY_DLQ_CONSUMER_GROUP:call-third-party-dlq-group}
        consume-thread-max: ${CALL_THIRD_PARTY_DLQ_CONSUMER_THREADS:1}
        max-reconsume-times: ${CALL_THIRD_PARTY_DLQ_MAX_RECONSUME_TIMES:3}
  outbox:
    batch-size: ${CALL_OUTBOX_BATCH_SIZE:100}
    max-retries: ${CALL_OUTBOX_MAX_RETRIES:10}
    poll-interval: ${CALL_OUTBOX_POLL_INTERVAL:PT5S}
    retry-backoff: ${CALL_OUTBOX_RETRY_BACKOFF:PT30S}
    processing-timeout: ${CALL_OUTBOX_PROCESSING_TIMEOUT:PT5M}
  postprocess:
    llm-enabled: ${CALL_LLM_ENABLED:false}
    topics:
      record-persisted: ${CALL_RECORD_PERSISTED_TOPIC:call_record_persisted}
      analysis-completed: ${CALL_RECORD_ANALYSIS_COMPLETED_TOPIC:call_record_analysis_completed}
  shard:
    db-count: ${CALL_DB_COUNT:4}
    table-count: ${CALL_TABLE_COUNT:16}
  id:
    machine-id: ${CALL_MACHINE_ID:1}
    epoch: ${CALL_ID_EPOCH:1700000000000}
  datasource:
    nodes:
      - index: 0
        host: ${MYSQL_HOST:localhost}
        port: ${MYSQL_PORT:3306}
        database: call_0
        username: ${MYSQL_USER:call}
        password: ${MYSQL_PASSWORD:call123}
      - index: 1
        host: ${MYSQL_HOST:localhost}
        port: ${MYSQL_PORT:3306}
        database: call_1
        username: ${MYSQL_USER:call}
        password: ${MYSQL_PASSWORD:call123}
      - index: 2
        host: ${MYSQL_HOST:localhost}
        port: ${MYSQL_PORT:3306}
        database: call_2
        username: ${MYSQL_USER:call}
        password: ${MYSQL_PASSWORD:call123}
      - index: 3
        host: ${MYSQL_HOST:localhost}
        port: ${MYSQL_PORT:3306}
        database: call_3
        username: ${MYSQL_USER:call}
        password: ${MYSQL_PASSWORD:call123}
  elasticsearch:
    uris:
      - ${ES_URI:http://localhost:9200}
    username: ${ES_USERNAME:elastic}
    password: ${ES_PASSWORD:elastic123}
    auto-init: true

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n"
