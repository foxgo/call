apiVersion: apps/v1
kind: Deployment
metadata:
  name: call-iam
  namespace: call-center
spec:
  replicas: 2
  selector:
    matchLabels:
      app: call-iam
  template:
    metadata:
      labels:
        app: call-iam
    spec:
      containers:
        - name: call-iam
          image: call-iam:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: call-center-config
            - secretRef:
                name: call-center-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 20
