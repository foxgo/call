apiVersion: apps/v1
kind: Deployment
metadata:
  name: call-task
  namespace: call-center
spec:
  replicas: 2
  selector:
    matchLabels:
      app: call-task
  template:
    metadata:
      labels:
        app: call-task
    spec:
      containers:
        - name: call-task
          image: call-task:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: call-center-config
            - secretRef:
                name: call-center-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 20
