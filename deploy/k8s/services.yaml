apiVersion: v1
kind: Service
metadata:
  name: call-ingestion
  namespace: call-center
spec:
  selector:
    app: call-ingestion
  ports:
    - port: 8081
      targetPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: call-search
  namespace: call-center
spec:
  selector:
    app: call-search
  ports:
    - port: 8082
      targetPort: 8082
